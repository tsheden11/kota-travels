---
layout: layouts/base.njk
title: Our Packages - Kota Travels
---

{% from "components/atoms/button.njk" import button %}
{% from "components/molecules/image-card.njk" import imageCard %}
{% from "components/organisms/cta-section.njk" import ctaSection %}

<div class="page-packages">

  {# ============================================================
     SECTION 1 - PAGE HEADING
     Figma: column, center-aligned, gap: 20px, padding: 0 60px
  ============================================================ #}

  <section class="packages-heading">
    <div class="packages-heading__inner">
      <h1 class="packages-heading__title">
        <span class="packages-heading__title--black">Our </span>
        <span class="packages-heading__title--green">Packages</span>
      </h1>
      <p class="packages-heading__body">
        Choose the journey that speaks to you - each one fully
        customizable to your pace, interests, and travel style.
      </p>
    </div>
  </section>


  {# ============================================================
     SECTION 2 - PACKAGE CARDS
     Figma: row, space-between, padding: 0 60px
     Loops over packages data
  ============================================================ #}

  <section class="packages-grid">
    <div class="packages-grid__inner">
      {% for slug, package in packages %}
        {{ imageCard({
          title: package.title,
          description: package.tagline,
          image: package.images[0],
          imageAlt: package.title,
          buttonText: "View Tour",
          buttonHref: "/packages/" + package.slug
        }) }}
      {% endfor %}
    </div>
  </section>


  {# ============================================================
     SECTION 3 - CTA
  ============================================================ #}

  {{ ctaSection({
    heading: "Don't see what you're looking for?",
    subtext: "We customize every tour to suit your interests, pace, and travel style. Get in touch and let's build your perfect Bhutan journey.",
    primaryBtn: "Contact Us",
    primaryHref: "/contact",
    secondaryBtn: "Customize Your Own",
    secondaryModal: "customize",
    image: "/images/rice-fields.jpg"
  }) }}

</div>